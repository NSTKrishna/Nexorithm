services:
  backend:
    restart: always
    build: ./Backend
    ports:
      - "5001:8000"
    env_file:
      - ./Backend/.env
frontend:
    image: rajatrsrivastav/nexorithm-frontend:latest 
    restart: always
    ports:
      - "80:80" 